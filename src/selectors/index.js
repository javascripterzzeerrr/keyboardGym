// component is Field
export const currentText = state => state.field.currentText;

export const carriage = state => state.field.carriage;

export const wrongCounter = state => state.field.wrongCounter;

export const pressCounter = state => state.field.pressCounter;

export const cntLetter = state => state.field.cntLetter;